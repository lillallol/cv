{
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "webpack",
		"spell-check": "cspell \"./**/*\"",
		"print-pdf": "node print-pdf.js",
		"commit": "npm run spell-check && npm run print-pdf && git add --all && git commit && git push origin master"
	},
	"devDependencies": {
		"cspell": "^4.1.2",
		"puppeteer": "^5.4.1"
	}
}
