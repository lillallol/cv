{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack",
    "spell-check": "cspell \"./**/*\"",
    "commit": "npm run spell-check && git add --all && git commit && git push origin master"
  },
  "devDependencies": {
    "cspell": "^4.1.2"
  }
}
